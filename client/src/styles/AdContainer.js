import styled from "styled-components";

const Header = styled.header`
  margin: 0;
  width: 100%;
  height: 37.5rem;
`

function AdContainer({ ...ads }) {
  
  return (
  <Header src={Image.url} alt={Image.explain} />
  )
}

export default AdContainer